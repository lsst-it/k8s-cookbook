defaultNamespace: &name openvpn-db
labels:
  bundle: *name
dependsOn:
  - name: openvpn-db-pre
    namespace: openvpn-db
kustomize:
  dir: base
targetCustomizations:
  - name: ruka
    clusterName: ruka
    kustomize:
      dir: overlays/ruka

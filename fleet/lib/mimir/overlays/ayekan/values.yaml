---
mimir:
  structuredConfig:
    common:
      storage:
        s3:
          endpoint: s3.o11y.${ .ClusterLabels.site }.lsst.org
    limits:
      compactor_blocks_retention_period: 90d

ingester:
  persistentVolume:
    size: 300Gi

apiVersion: v1
kind: Service
metadata:
  name: mariadb-galera
  namespace: openvpn-db
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mariadb
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306

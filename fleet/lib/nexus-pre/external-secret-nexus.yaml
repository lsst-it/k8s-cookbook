---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nexus-license
  namespace: nexus
spec:
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: nexus-license
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: nexus-pro-license

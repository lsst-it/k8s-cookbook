---
apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: latiss-raw
  namespace: rook-ceph
spec:
  metadataPool:
    failureDomain: host
    replicated:
      size: 3
    quotas:
      maxSize: 50Gi
  dataPool:
    failureDomain: host
    erasureCoded:
      dataChunks: 2
      codingChunks: 1
    quotas:
      maxSize: 1.01Ti
  preservePoolsOnDelete: false
  gateway:
    sslCertificateRef:
    port: 80
    instances: 3
    resources:
      limits:
        cpu: "4"
        memory: 4Gi
      requests:
        cpu: "4"
        memory: 4Gi
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rook-ceph-rgw-ingress-latiss-raw
  namespace: rook-ceph
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: 1024m
spec:
  tls:
  - hosts:
    - latiss-raw.ls.lsst.org
    secretName: rook-ceph-rgw-ingress-tls-latiss-raw
  rules:
  - host: latiss-raw.ls.lsst.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rook-ceph-rgw-latiss-raw
            port:
              number: 80

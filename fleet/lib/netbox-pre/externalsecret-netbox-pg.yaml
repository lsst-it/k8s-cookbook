---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: netbox-pg
  namespace: netbox
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: netbox-pg
        property: username
    - secretKey: password
      remoteRef:
        key: netbox-pg
        property: password

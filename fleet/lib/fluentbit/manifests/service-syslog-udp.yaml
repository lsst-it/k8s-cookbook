---
apiVersion: v1
kind: Service
metadata:
  name: fluent-bit-syslog-udp
  namespace: fluent-bit
spec:
  selector:
    app.kubernetes.io/name: fluent-bit
  ports:
    - protocol: UDP
      port: 5140
      targetPort: 5140
  type: LoadBalancer

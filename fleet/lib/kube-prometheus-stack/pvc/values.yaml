---
prometheus:
  prometheusSpec:
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: [ReadWriteOnce]
          storageClassName: rook-ceph-block
          resources:
            requests:
              storage: 50Gi

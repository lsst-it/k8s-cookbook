---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rook-ceph-demo-pvc1
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
